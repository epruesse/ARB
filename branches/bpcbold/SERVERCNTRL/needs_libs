# libraries needed to link against SERVERCNTRL/SERVERCNTRL.a

ARBDB/libARBDB.so

# it does not seem to matter which client.a is linked
# (e.g. arb_ntree only links PROBE_COM/client.a, so we always use that one)
PROBE_COM/client.a
# NAMES_COM/client.a

