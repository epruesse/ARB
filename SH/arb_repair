#!/bin/sh

if [ -z "$2" ]; then
    echo "Usage: arb_repair database outputdatabase"
else
    echo "Repairing database $1 .. get yourself a coffee. :)"
    echo ""
    arb_2_ascii $1 - | arb_2_bin - $2
    ls -al $1 $2
fi
