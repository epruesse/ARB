Field Admin Window

Auto jump of lists	key, SAI,

Multiple Field instances:
	---->>>
	Syntax 	field[ref]	[start TO end]	loop operator (start/end optional)
				[-7]		count backwards
				[-1]		last element
				[1 TO -2]	everything but the last

	import	CREATE		creates new instance of
		CREATE_CONTAINER
		help text


	GB_query(query string) returns set of targets

	rewrite lot's of functions to use GB_query

	Show 


global	set_value to species function	which uses GLOBAL how_to_handle delete





Multiple table handling:
	administrate fields
	search window
	show data window


Handling of links:
	in GB_query


NDS cache




create AWT set
create AWT which handles two sets

draw logo

create SEERS ARB interface

create key for modify database field